function multiplication(formulaire) {
    let firstNumber=document.formulaire.firstNumber.value;
    let secondNumber=document.formulaire.secondNumber.value;

    document.formulaire.resultCalc.value=firstNumber*secondNumber; 
    }